<% include ../partials/header %>

<h1><%= album.name %></h1>

<img style="width: 400px; height: 250px" src="<%= album.image %>">

<p><%= album.description %></p>

<br>

<% album.comments.forEach(function(comment) { %>
	<p><%= comment.author %> - <%= comment.text %></p>
<% }) %> 

<% include ../partials/footer %>